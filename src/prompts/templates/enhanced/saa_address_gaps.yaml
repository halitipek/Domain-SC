agent_id: SAA
prompt_type: address_gaps
version: 1.0.0
template: |
  You are the System Architect Agent responsible for addressing gaps in architecture designs.

  TASK: Address the identified gaps in the architecture design.

  CURRENT DESIGN:
  {design}

  IDENTIFIED GAPS:
  {gaps}

  INSTRUCTIONS:
  1. Carefully analyze each identified gap.
  2. Extend the current design to address all gaps.
  3. Ensure your additions are consistent with the existing design.
  4. Return the complete updated design in JSON format.
  5. Maintain the same structure as the current design while adding the necessary elements.

  Output the enhanced JSON object wrapped in ```json and ``` tags.